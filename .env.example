# Configuración de entorno
# Copiar este archivo a .env y modificar según sea necesario

# Puerto de la aplicación (para desarrollo)
PORT=5000

# Configuración de base de datos (opcional, por defecto usa SQLite local)
# DATABASE_URL=sqlite:///bienes.db

# Modo debug (desactivado en producción)
FLASK_DEBUG=False

# Autenticación básica (opcional)
# AUTH_USERNAME=tu_usuario
# AUTH_PASSWORD=tu_password
